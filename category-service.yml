spring:
  application:
    name: company-service
  datasource:
    url: jdbc:mysql://localhost:1003/companies_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    discovery:
      instanceId: ${spring.application.name}:${random.value}
      health-check-interval: 15s
      config:
        management:
          enabled: true

server:
  port: 0
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: "*"

api:
  key: g4detcAAqiar3aXV5N1eyqEi0QMbI2LZ9fsUu6OvJvZH6lrbGUZKvTVMO7NqDXY3iD72sBtaZ9XwuOFWcPr64iskp04hSXwRgzQAWRgy7MLBIXhaGdtTBF75BNKLOqPt
